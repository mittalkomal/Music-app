<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.komal.mymusicapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SufiActivity"
            android:label="@string/sufi" />
        <activity
            android:name=".PopActivity"
            android:label="@string/pop" />
        <activity
            android:name=".GazalActivity"
            android:label="@string/menu3" />
        <activity
            android:name=".AllActivity"
            android:label="@string/all" />
        <activity android:name=".PaymentActivity"
            android:label="@string/settings"/>
    </application>

</manifest>